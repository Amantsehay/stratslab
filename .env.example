# Database Configuration
# PostgreSQL connection string with asyncpg driver
# Format: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/stratslab_dev

# Security & Encryption
# Secret key for encryption, JWT signing, and session management
# Must be at least 32 characters for secure encryption
SECRET_KEY=your-secret-key-here-change-in-production

# Server Configuration
# Trusted host for URL generation and security
TRUSTED_HOST=localhost

# CORS Configuration
# Comma-separated list of allowed origins for cross-origin requests
ALLOWS_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000

# Database Connection Pool Settings
# Number of connections to keep in the connection pool
POOL_SIZE=5

# Maximum number of overflow connections beyond POOL_SIZE
POOL_MAX_OVERFLOW=10

# Connection timeout in seconds
POOL_TIMEOUT=30

# Recycle connections after N seconds (-1 = no recycling)
POOL_RECYCLE=-1

# Feature Flags
# Enable HTTPS redirect
ENABLE_HTTPS_REDIRECT=false

# Enable email sending functionality
SEND_EMAILS=false

# Auto-activate new users
ACTIVATE_USERS=false

# Enable Sentry error tracking
ENABLE_SENTRY=false

# Enable API request counting middleware
COUNT_API_REQUESTS=false

# API Documentation Configuration
# URL path for Swagger UI documentation
DOCS_URL=/api-docs

# URL path for ReDoc alternative documentation
REDOC_URL=/redoc

# URL path for OpenAPI JSON schema
OPENAPI_URL=/openapi.json
<<<<<<< HEAD

# Agentic Developer Workflow (ADW) Configuration
# GitHub repository URL for ADW integration
GITHUB_REPO_URL=https://github.com/owner/repository

# Anthropic API Key (required for Claude Code CLI)
# Get from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-

# Path to Claude Code CLI executable
# Defaults to "claude" if in PATH, or provide full path
CLAUDE_CODE_PATH=claude

# GitHub Personal Access Token (optional, only if using different account than 'gh auth login')
# Create at https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_PAT=

# ADW Debug Mode (set to "true" for verbose logging)
ADW_DEBUG=false

# Webhook Configuration
# Port for webhook server (default: 8001)
WEBHOOK_PORT=8001

# Webhook secret for GitHub signature verification
WEBHOOK_SECRET=

# ADW Execution Configuration
# Maximum timeout for workflow execution in seconds (default: 1800 = 30 minutes)
ADW_TIMEOUT=1800

# Model to use for agentic workflows (sonnet or opus)
# sonnet: Faster, lower cost (recommended for most tasks)
# opus: More capable, higher cost (for complex tasks)
ADW_MODEL=sonnet
=======
>>>>>>> main
